{
  "name": "opencli-njust",
  "version": "0.1.0",
  "private": false,
  "description": "NJUST extension for OpenCLI, starting with schedule query support.",
  "type": "module",
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "scripts"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "powershell -NoProfile -Command \"if (Test-Path dist) { Remove-Item -Recurse -Force dist }\"",
    "prepare:release": "node ./scripts/prepare-release.mjs",
    "pack:release": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/package-release.ps1",
    "test": "vitest run",
    "test:watch": "vitest",
    "sync:opencli": "node ./scripts/sync-opencli.mjs"
  },
  "peerDependencies": {
    "@jackwener/opencli": "^1.6.7"
  },
  "devDependencies": {
    "@jackwener/opencli": "^1.6.7",
    "@types/node": "^24.5.2",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
